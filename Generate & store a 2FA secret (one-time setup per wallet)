const speakeasy = require("speakeasy");

function generate2FASecret() {
  const secret = speakeasy.generateSecret({
    name: "MyWalletApp"
  });

  return {
    base32: secret.base32,   // store this securely in DB
    otpauth_url: secret.otpauth_url
  };
}
